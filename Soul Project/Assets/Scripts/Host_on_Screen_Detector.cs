using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Host_on_Screen_Detector : MonoBehaviour

{
    [SerializeField]
    internal HostInputScript hostInputScript;
    [SerializeField]
    internal HostMovementScript hostMovementScript;

    public HostController[] host_List;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
